// คัดลอกเป็น secrets.h แล้วกรอกค่าจริง
// ห้ามกรอกค่าจริงในไฟล์นี้ มันถูกอัปขึ้น GitHub · secrets.h อยู่ใน .gitignore แล้ว
#pragma once

#define WIFI_SSID      "ชื่อ WiFi ของคุณ"
#define WIFI_PASSWORD  "รหัสผ่าน WiFi"

// Firebase Console → Project settings → Service accounts → Database secrets
#define FIREBASE_AUTH  "วาง database secret ที่นี่"

// URL จากหน้า Realtime Database ตัด https:// กับ / ท้ายออก
// ต้องตรงกับ FIREBASE_DOMAIN ฝั่งเว็บเป๊ะ ๆ
#define FIREBASE_HOST  "xxxxx-default-rtdb.asia-southeast1.firebasedatabase.app"
